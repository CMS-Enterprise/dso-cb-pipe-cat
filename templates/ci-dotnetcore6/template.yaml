version: 1
type: pipeline-template
templateType: MULTIBRANCH
name: Dev-dotNet Core 6 Pipeline Job
description: Simple dotnetcore 6 App
parameters:
  - name: email_recipient
    type: string
    displayName: Email address to be used for sending build notifications
  - name: github_repo
    type: string
    displayName: GitHub Repo Name
  - name: gh_org
    type: string
    displayName: Name of your Github Org
  - name: artifact_name
    type: string
    displayName: Name of your artifact in Artifactory
  - name: artifactory_project_name
    type: string
    displayName: Name of your project in Artifactory
  - name: artifact_package_path
    type: string
    displayName: Path to your artifact (required if zip/binary upload)
  - name: artifactory_path
    type: string
    displayName: URL to Artifact in Jfrog Artifactory or ECR (required if image upload)
  - name: ado_iam_role
    type: string
    displayName: IAM Role ARN to be assumed in ADO owned AWS account (required if using ECR)
  - name: build_args
    type: string
    displayName: dotnet Build arguments
    defaultValue: build
  - name: test_args
    type: string
    displayName: dotnet Test arguments
    defaultValue: test
  - name: sq_project_key
    type: string
    displayName: SonarQube project key
  - name: org_id
    type: string
    displayName: Snyk Org ID
  - name: docker_args
    type: string
    displayName: Docker arguments
  - name: work_directory
    type: string
    displayName: Relative location of the application working directory
    defaultValue: .
  - name: docker_file
    type: string
    displayName: Name of the Dockerfile
  - name: zip_path
    type: string
    displayName: Relative location of the application files to be uploaded to Artifactory (required if zip upload)
    defaultValue: .
  - name: file_name
    type: string
    displayName: Name of the build zip file (required if zip upload)
    defaultValue: example.zip
   
multibranch:
  branchSource:
    # git:
      # remote: ${github_repo}
    github:
      repoOwner: ${gh_org}
      repository: ${github_repo}
      credentialsId: "git_token"
  orphanedItemStrategy:
    daysToKeep: 3